#!/usr/bin/env fish

rm -rf .generated && mkdir .generated
for f in (fd --extension txt)
  set -l newfile .generated/(basename "$f" ".txt").md
  # cp $f $newfile
  # gawk -i inplace '/---/{x++} x==2{sub(/---/,"&\n```")}1' $newfile
  echo "$f" "->" "$newfile"
  pandoc -s $f -o $newfile
end

