function bat -d 'bat limited by size'
  for argv in $argv
    if [ -f "$argv" ]
      set -l f (fd --max-depth 1 "^$argv"'$' --size +1M)
      if not [ -z $f ]
        ls -alh $argv
        return
      end
    end
  end
  command bat $argv
end
