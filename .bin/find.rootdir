#!/usr/bin/env elvish
#
# find rootdir where $args[0] is located
#
# find.rootdir .git

var target = $args[0]

for i [(range 20)] {
    if ?(test -e $target) {
      pwd
      exit 0
    } else {
      cd ..
    }
}

exit 1


