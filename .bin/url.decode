#!/usr/bin/env python3

import urllib.parse
import json
import sys

if len(sys.argv) == 1:
    data = sys.stdin.readline()
else:
    data = sys.argv[1]

import sys, urllib.parse as ul
print(ul.quote_plus(data))
