#!/usr/bin/env fish

if [ (count $argv) -ne 2 ]
    echoerr "not two argument"
    exit 1
end

for arg in $argv
    if [ ! -d "$arg" ]; echoerr "not directory $arg"; exit 1; end
end

command mv -v -n $argv[1]/* $argv[2]/


