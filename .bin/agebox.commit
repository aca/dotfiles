#!/bin/bash

set -euo pipefail

cd $(git rev-parse --show-toplevel 2>/dev/null) 
git reset --mixed
agebox.reencrypt
agebox reencrypt
git add **/**.agebox .ageboxreg.yml keys
git commit --allow-empty-message -m ""
