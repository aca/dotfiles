#!/usr/bin/env elvish

to-lines | order &less-than={|a b| > 0 (compare (basename $a) (basename $b)) } | mpv --playlist=- --msg-level=file=no $@args
# to-lines | order &less-than={|a b| > 0 (compare (basename $a) (basename $b)) } | mpv --playlist=- --msg-level=file=no $@args
